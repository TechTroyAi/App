<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera captures land under getExternalFilesDir(null); the provider only
         exposes files below that directory. -->
    <external-files-path
        name="captures"
        path="." />
    <cache-path
        name="exports"
        path="exports/" />
</paths>

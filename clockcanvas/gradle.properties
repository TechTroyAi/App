org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

# ClockCanvas uses AndroidX (Compose preview screen). This file is scoped to
# this composite build only - the sibling Blockhold/Jadex app in the parent
# repository still builds with android.useAndroidX=false.
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
